# NFL Squares

NFL squares pool web application.

> Shared context for both Claude Code and Codex. The `CLAUDE.md` in this folder is a symlink to this file — edit `AGENTS.md` only so both CLIs stay in sync.

## Inherited conventions

- Global house style: `~/.codex/AGENTS.md` (Codex) and `~/.claude/CLAUDE.md` (Claude Code).
- New work stays under `~/Projects`; never `~/Documents`.
- Active host is the M4 Pro **Mac mini** workhorse (`Mac-mini-2.local`), distinct from the Intel MacBook Pro.

## Stack

- Python, Django, Docker

## Notes

- _Fill in project-specific gotchas, conventions, or TODOs as the project evolves._
